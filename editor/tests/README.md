# Editor Tests

This folder is reserved for editor-specific tests.

Planned buckets:

- `unit/` for scene graph, serialization, property, and layout rules
- `roundtrip/` for sidecar save-load-export checks
- `export/` for generated C23 golden tests
- `runtime_smoke/` for built scene behavior checks
