# Screenshots

This directory now has real local captures for application material and README embedding:

- `node_graph_demo.png`
- `text_editor_mini.png`
- `calculator_mini.png`

Still worth adding later:

- `perf_overlay.png` - perf widget showing frame time graph
- `mtsdf_text_zoom.png` - MTSDF text at 3 zoom levels vs raster

Current recommendation:

- replace `node_graph_demo.png` only if we decide to stage a denser hero shot
- `node_graph_demo.png` is now captured from the demo's `--hero-shot` mode so labels stay visible
