# Showcase

Presentation-grade applications for Stygian.

Layout:
- root: built `.exe` outputs only
- `apps/`: one folder per app, with source plus any app-local build and test files

The shared borderless starter app lives in `apps/template/`.
